import PlaceholderNav from "../placeholder-nav";

export const metadata = { title: "Layers | SleepOS" };

export default function LayersPage() {
  return (
    <main className="placeholder-page">
      <PlaceholderNav />
    </main>
  );
}
